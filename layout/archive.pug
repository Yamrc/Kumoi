extends _partial/layout

block content
	h1= __('page.archives.title')
	if site && site.posts && site.posts.length
		- const list = site.posts.sort('date', -1).toArray()
		include _partial/mixins/article_sort
		+articleSort(list)
	else
		p No posts
